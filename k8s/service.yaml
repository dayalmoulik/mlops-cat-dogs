apiVersion: v1
kind: Service
metadata:
  name: cats-dogs-service
  labels:
    app: cats-dogs-classifier
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: cats-dogs-classifier
